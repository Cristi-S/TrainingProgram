unit UEnum;
// модуль с перечислениями, используемыми во всем приложении

interface

type
  TListState = (lsNormal, lsAddbefore, lsAddAfter, lsDelete);
  TOperatingMode = (omControl, omNormal, omDemo);

implementation

end.
